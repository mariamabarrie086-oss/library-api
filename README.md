# library-api
The Library API is a simple system that manages basic library operations such as viewing books, borrowing, returning, and checking fines. It simulates how users interact with a library through simple API requests.
